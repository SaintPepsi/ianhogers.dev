---
// Gallery.astro — full-bleed collage photo gallery
// Usage: <Gallery> <GalleryGroup title="Section"> ![alt](/path.webp) </GalleryGroup> </Gallery>
// Photos get collage-style rotations, overlap, and hover-to-front behavior.
// Pure CSS — no JS required.
---

<div class="gallery-breakout">
  <div class="gallery-collage">
    <slot />
  </div>
</div>

<style>
  .gallery-breakout {
    width: 100vw;
    margin-left: calc(-50vw + 50%);
    padding: 2rem 1.5rem;
    background:
      radial-gradient(ellipse at 30% 50%, rgba(245, 158, 11, 0.04) 0%, transparent 60%),
      radial-gradient(ellipse at 70% 30%, rgba(179, 136, 255, 0.03) 0%, transparent 50%);
  }

  .gallery-collage {
    max-width: 72rem;
    margin: 0 auto;
  }

  @media (min-width: 1024px) {
    .gallery-breakout {
      padding: 2.5rem 2rem;
    }
  }
</style>
