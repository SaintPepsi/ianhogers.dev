---
// Gallery.astro — full-bleed collage container
// Groups flow horizontally, filling page width. Each group is a cluster.
// Usage: <Gallery> <GalleryGroup title="..."> <CollageItem>...</CollageItem> </GalleryGroup> </Gallery>
---

<div class="gallery-breakout">
  <div class="gallery-collage">
    <slot />
  </div>
</div>

<style>
  .gallery-breakout {
    width: 100vw;
    margin-left: calc(-50vw + 50%);
    padding: 2rem 1.5rem;
    background:
      radial-gradient(ellipse at 30% 50%, rgba(245, 158, 11, 0.04) 0%, transparent 60%),
      radial-gradient(ellipse at 70% 30%, rgba(179, 136, 255, 0.03) 0%, transparent 50%);
    overflow: visible;
  }

  .gallery-collage {
    max-width: 80rem;
    margin: 0 auto;
    display: flex;
    flex-wrap: wrap;
    align-items: flex-start;
    gap: 1rem;
  }

  @media (max-width: 767px) {
    .gallery-collage {
      flex-direction: column;
      align-items: center;
    }
    .gallery-breakout {
      padding: 1.5rem 1rem;
    }
  }

  @media (min-width: 1024px) {
    .gallery-breakout {
      padding: 2.5rem 2rem;
    }
  }
</style>
